# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Root node_modules (leftover from web/ move — safe to delete)
node_modules/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── web/ ──────────────────────────────────────────────
web/node_modules/
web/dist/
web/dist-ssr/
web/*.local

# ── mobile/ ───────────────────────────────────────────
# Environment — NEVER commit real keys
mobile/.env
mobile/.env.local
mobile/.env.production
mobile/.env*.local

# Firebase native config (iOS — store in EAS Secrets when building for iOS)
mobile/GoogleService-Info.plist

# Expo / Metro
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/expo-env.d.ts
mobile/.metro-health-check*

# Native build artifacts (generated by EAS Build)
mobile/.kotlin/
mobile/ios/
mobile/android/
mobile/*.jks
mobile/*.p8
mobile/*.p12
mobile/*.key
mobile/*.mobileprovision
mobile/*.pem
mobile/*.tsbuildinfo
